#include "main.h"
/**
 * mul - func checks for a digit (0 through).
 * @a: a - Variable
 * @b: b - Variable
 * Return: Always 0 (success)
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
